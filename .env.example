# Variáveis públicas (usadas no cliente)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Variáveis privadas (somente servidor)
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_EMAIL=admin@bingo.com
ADMIN_PASSWORD=admin123

# Gere um segredo forte para produção
# Exemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=